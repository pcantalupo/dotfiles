
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

alias h="history"
alias j="jobs"

alias jr='joe README.txt'
alias cr='cat README.txt'

alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."


HNAME=$(hostname)
HTC='htc'
if [[ "$HNAME" == *"$HTC"* ]]; then
  alias tree="/ihome/crc/install/tree/tree-1.8.0/tree"
fi

if [[ "$HNAME" == *"$HTC"* || "$HNAME" == *bridges2* ]]; then
  alias status='echo "Tmux"; tmux ls; echo -e "\nPOSIX Jobs"; j; echo -e "\nSLURM Jobs"; squeue -u $USER -S S -o "%8i %75j %2t %12l %8M %4C %.R %S"'
fi

if [[ "$HNAME" == "Rigel" ]]; then
  # see https://datascienceatthecommandline.com/2e/chapter-7-exploring-data.html#chapter-7-exploring-data
  alias rush='/Library/Frameworks/R.framework/Versions/Current/Resources/library/rush/exec/rush'
fi

# open like with mac
if [[ "$HNAME" == "sirius" ]]; then
  alias open='cygstart'
fi




# LS stuff
# Detect which `ls` flavor is in use
if ls --color > /dev/null 2>&1; then # GNU `ls`
	colorflag="--color"
else # OS X `ls`
	colorflag="-G"
fi
# List all files colorized in long format
alias l="ls -lF ${colorflag}"
# List all files colorized in long format, including dot files
alias la="ls -lAF ${colorflag}"
# List only directories
alias lsd="ls -lAF ${colorflag} | grep --color=never '^d'"
# Always use color output for `ls`
alias ls="command ls ${colorflag}"


# Enable aliases to be sudo’ed
alias sudo='sudo '

# Show/hide hidden files in Finder
alias show="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"
alias hide="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"

# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"


